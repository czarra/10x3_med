{% extends 'base.html.twig' %}

{% block title %}Twój profil{% endblock %}

{% block body %}
    <h1>Edytuj swoje parametry</h1>

    {% for message in app.flashes('success') %}
        <p>{{ message }}</p>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Zapisz</button>
    {{ form_end(form) }}
{% endblock %}
